## Live Preview side by side

`Press Ctrl + K then V`

## Project start

` npm init -y` <br>
`npm install express --save`

`npm install -D typescript`
` npx tsc --init`
`  "rootDir": "./src",
    "outDir": "./dist",`
`"sourceMap": true,
    "declaration": true,
    "declarationMap": true,`
//dir+dist on //other output comment out off
`"verbatimModuleSyntax": true,`
`"jsx": "react-jsx",`

## hellow word

`npm i --save-dev @types/express`

### npx tsx example.ts

`npm i -D tsx`

## Install postgresssql

`npm i pg` || `npm install postgres`
`npm i --save-dev @types/pg`

### DB store NEON db



`npm i dotenv`
`.gitignore`

# express_crud_postgresssql
